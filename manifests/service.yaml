apiVersion: v1
kind: Service
metadata:
  name: test-aks-auto
  namespace: np-aksautomation-testzw
  labels:
    kubernetes.azure.com/generator: devhub
spec:
  type: LoadBalancer
  selector:
    app: test-aks-auto
  ports:
    - protocol: TCP
      port: 3000
      targetPort: 3000